# Backend API Configuration
VITE_BACKEND_URL=http://localhost:3000

# Game Configuration (optional overrides)
# These are typically managed by the backend, but can be set for testing
VITE_DEBUG_MODE=false
